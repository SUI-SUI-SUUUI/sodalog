# gas

Google Apps Script用のコードを管理するフォルダです。

## 予定ファイル

- Code.gs
- config.example.gs
- line.gs
- sheet.gs
- drive.gs
- parser.gs

## 注意

LINEのチャネルアクセストークン、チャネルシークレット、スプレッドシートID、DriveフォルダIDなどの秘密情報はGitHubに載せません。

公開する場合は、実際の値ではなく `config.example.gs` のようなサンプルだけを置きます。
